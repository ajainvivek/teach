import { teachRevealRender } from '../../../helpers/teach-reveal-render';
import { module, test } from 'qunit';

module('Unit | Helper | teach reveal render');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = teachRevealRender(42);
  assert.ok(result);
});
